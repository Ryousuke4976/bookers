<h1>Editing Book</h1>
<%= form_for(@booker) do |f| %>
  <label>Title</label>
  <%= f.text_field :title %>

  <label>Body</label>
  <%= f.text_area :body %>

  <%= f.submit "Update Book" %>
<% end %>

<%= link_to "Show", booker_path(@booker.id) %>
<%= link_to "Back", bookers_path %>